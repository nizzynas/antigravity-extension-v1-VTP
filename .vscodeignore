# Source & dev files (not needed at runtime — webpack bundles everything)
src/
out/
.vscode-test/
tsconfig.json
webpack.config.js
.gitignore
.env
*.vsix

# All node_modules are webpack-bundled into dist/extension.js
node_modules/**
